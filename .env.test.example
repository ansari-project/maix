########################################################
# 🧪 TEST DATABASE CONFIGURATION 🧪
# ⚠️  THIS IS FOR INTEGRATION TESTS ONLY! ⚠️
########################################################
# 
# 🗄️ USING TEST DATABASE ON PORT 5433 (not 5432!)
# ✅ This connects to Docker test database
# ❌ NEVER point this to production database!
#
# To start test database: npm run test:db:start
# To check status: npm run test:db:status
# To run tests safely: npm run test:integration:safe
########################################################

# Test Database - Should be a separate database from development/production
# For local Docker test database, use:
DATABASE_URL="postgresql://testuser:testpass@localhost:5433/maix_test"
# For CI or remote test database, update with your test database URL

# NextAuth.js for testing
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="test-secret-only-for-testing"

# Test configuration flags
NODE_ENV="test"
IS_TEST_ENV="true"

# Disable external services in tests
DISABLE_EMAIL="true"
DISABLE_EXTERNAL_APIS="true"

# Mock API keys for testing (not real keys)
ANTHROPIC_API_KEY="test-anthropic-key"
GEMINI_API_KEY="test-gemini-key"
RESEND_API_KEY="test-resend-key"

# Test encryption key (consistent for test reproducibility)
ENCRYPTION_KEY="test-encryption-key-32-characters!!"